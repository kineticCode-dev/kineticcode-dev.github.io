{
  "name": "kinetic-code-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Static site generator for Kinetic Code — plain HTML/CSS/JS output, built from Markdown.",
  "scripts": {
    "build": "node scripts/build.js",
    "serve": "npx --yes http-server dist -p 8080 -c-1",
    "translate": "node scripts/translate.js"
  },
  "devDependencies": {
    "gray-matter": "^4.0.3",
    "highlight.js": "^11.12.0",
    "katex": "^0.18.4",
    "marked": "^12.0.2",
    "marked-katex-extension": "^5.1.12"
  }
}
